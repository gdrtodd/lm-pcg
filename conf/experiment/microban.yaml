# @package _global_
hydra:
  sweeper:
    params:
      # seed: range(0, 5)
      # seed: range(5, 10)
      seed: range(0, 10)
      source: microban, microban_flips, microban_flips_rotations

exp_name: microban