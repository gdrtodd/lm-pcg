defaults:
  - config
  - _self_

  # - override hydra/launcher: submitit_local
  - override hydra/launcher: submitit_slurm


num_eval_proc: 10  # For computing solutions in parallel


hydra:
  # SLURM-specific arguments
  launcher:
    tasks_per_node: 1
    cpus_per_task: 10
    gpus_per_node: 1
    timeout_min: 100
    mem_gb: 30